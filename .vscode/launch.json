{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run OpenCode Native",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "Build OpenCode Native"
    }
  ]
}
